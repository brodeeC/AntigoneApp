# Flask API (install into a venv: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt)
flask>=3.0.0
flask-sqlalchemy>=3.0.0
flask-limiter>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
pytest>=8.0.0
